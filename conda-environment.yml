name: veros

channels:
    - conda-forge

dependencies:
    - python=3.12
    - pip
    - setuptools
    - click
    - entrypoints
    - requests
    - numpy
    - scipy
    - h5netcdf
    - h5py
    - pillow
    - loguru
    - tqdm
    - cython
    - pip:
        - -e .
